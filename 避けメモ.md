## button


```css

 &:hover::before{
    transform: scaleX(0) ;
   }

 &:hover::after{
    transform: scaleY(0) ;
   }

  > span {
    cursor: pointer;
    display: block;
    text-align: center;
    width: 100%;
    height: 100%;
    position: relative;
    background: #000;
    margin: 0;
    padding: 8px;
    z-index: 3;
  }

  >span::before {
    height: ${commonStyle.height};
    width:  ${commonStyle.width} - (${commonStyle.height} / 2) ;
    top: -1px;
    left: ${commonStyle.height} / 4;
    }
    
  >span::after {
    height: ${commonStyle.height} / 2;
    width: ${commonStyle.width};
    top: ${commonStyle.height} / 4;
    left: -1px;
  }

  >span::after,  >span::before {
  position: absolute;
  transition: 1s;
  background: ${Styles.COLOR.WHITE};
  content: 'あいうえお';
  z-index: -1;
    }
  >span, >span::before, >span::after {
    box-sizing: border-box;
  }
```